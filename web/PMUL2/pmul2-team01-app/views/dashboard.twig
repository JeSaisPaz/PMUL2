{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  <p>Welcome to {{title}}</p>
  <button class="receive-btn" 
        data-order="{{ orderId }}" 
        data-color="{{ colorId }}">
    Mark as Received
</button>
<script>
</script>
{% endblock %}
